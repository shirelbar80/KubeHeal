# KubeHeal runtime dependencies
kubernetes>=29.0.0        # official Python K8s client (watch, patch, logs)
ollama>=0.3.0             # local LLM client (native format=json)
slack-bolt>=1.18.0        # Slack app framework (Socket Mode)
pydantic>=2.6.0           # data models + LLM output validation
pydantic-settings>=2.2.0  # .env-backed config
fastapi>=0.110.0          # optional internal health/metrics endpoint
uvicorn>=0.29.0           # ASGI server for FastAPI

# Dev / test
pytest>=8.0.0
